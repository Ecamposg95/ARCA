[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning:passlib.*
markers =
    postgres: requiere PostgreSQL real (se omite en modo SQLite)
